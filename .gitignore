# 测试音频/视频文件（含版权内容，绝不提交）
test/*
!test/test_*.py
captured.wav
*.wav
*.mp3
*.mp4
*.m4a
*.ogg
*.flac

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
.env

# 打包产物（PyInstaller 输出的发行包，体积大且每次构建不同）
sub-title-*.zip

# 模型缓存（FunASR 首次运行下载到此处）
.cache/

# IDE
.vscode/
.idea/
*.swp

# ZCode 工具内部文件
.zcode/

# 用户运行时主题目录（运行 app 后自动产生）
themes/

# 用户配置 + 凭证（运行时数据，绝不入库）
config.yaml
credentials.json
